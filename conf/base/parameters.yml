target_column: "Potability"

split:
  test_size: 0.30
  val_ratio: 0.50
  random_state: 42

model:
  n_estimators: 300
  random_state: 42
  class_weight: "balanced"
  n_jobs: -1

imputer:
  strategy: "median"
